# Infra_Pulse - ML Pipeline Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # For file uploads (issue images)

# ── ML / Embeddings ────────────────────────────────────────────────────────────
torch==2.3.0                   # Deep learning backend
torchvision==0.18.0
transformers==4.41.0           # HuggingFace: CLIP, DistilBERT
Pillow==10.3.0                 # Image handling
numpy==1.26.4
scikit-learn==1.4.2            # cosine_similarity, preprocessing

# ── Geospatial ─────────────────────────────────────────────────────────────────
shapely==2.0.4                 # Polygon geofencing

# ── Data & Serialization ───────────────────────────────────────────────────────
pydantic==2.7.1
python-dotenv==1.0.1           # .env config loading
httpx==0.27.0                  # For inter-service calls

# ── Persistence (lightweight, no external DB required) ────────────────────────
aiofiles==23.2.1               # Async file I/O for embedding persistence